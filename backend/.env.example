PORT=5000
# Paste the complete Firebase service-account JSON on one line. Never commit the real value.
FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account"}
# Use a long random value and copy it into the ESP32 config.h file.
DEVICE_API_TOKEN=replace-with-a-long-random-secret
